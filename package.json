{
  "name": "pema",
  "scripts": {
    "start": "parcel ./src/index.html --out-dir ./docs --public-url / --port 1114",
    "prebuild": "cd docs && pwd && find . ! -name 'CNAME' -type f -exec rm -f {} +",
    "build": "parcel build ./src/index.html --out-dir ./docs --public-url / --no-source-maps"
  },
  "license": "MIT",
  "dependencies": {
    "airtable": "^0.5.5",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "parcel-bundler": "^1.8.1",
    "react": "^16.3.2",
    "react-dom": "^16.3.2"
  }
}
